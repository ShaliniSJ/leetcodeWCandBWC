#MINE
class Solution:
    def countDigits(self, num: int) -> int:
        num1 = str(num)
        count = 0
        for i in range(len(num1)):
            if num%int(num1[i]) == 0:
                count += 1
        return count
